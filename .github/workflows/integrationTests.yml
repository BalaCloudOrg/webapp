name: integrationTestsCI

on:
  workflow_dispatch:

jobs:
  integration_tests:
    name: Integration_tests job
    runs-on: ubuntu-latest
    env:
      PORT: "3000"
      MYSQL_HOST: "127.0.0.1"
      MYSQL_USER: "user1"
      MYSQL_PASSWORD: "Abcd@123"
      MYSQL_DATABASE: "test"

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Install API dependencies
        run: npm ci

      - name: Start mysql
        run: sudo /etc/init.d/mysql start

      - name: Create and set database
        run: |
          mysql -e "CREATE DATABASE IF NOT EXISTS $MYSQL_DATABASE;" -uroot -proot
          mysql -e "CREATE USER IF NOT EXISTS '$MYSQL_USER'@'localhost' IDENTIFIED BY '$MYSQL_PASSWORD';" -uroot -proot
          mysql -e "GRANT ALL PRIVILEGES ON $MYSQL_DATABASE.* TO '$MYSQL_USER'@'localhost';" -uroot -proot
          mysql -e "FLUSH PRIVILEGES;" -uroot -proot

      - name: Run integration tests
        run: npm run test

      - name: Delete the Database
        run: mysql -e "DROP DATABASE $MYSQL_DATABASE;" -uroot -proot
